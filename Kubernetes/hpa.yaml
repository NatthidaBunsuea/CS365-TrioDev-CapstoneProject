apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tetris-app-hpa
  namespace: tetris-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tetris-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
